import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  boxButton: {
    textAlign: 'center',
    paddingTop: '10px',
  },
  typography: {
    color: '#000',
    fontWeight: 400,
  },
}));

export default useStyles;
